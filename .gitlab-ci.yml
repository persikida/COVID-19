image: registry.services.mts.ru/docker/nginx.nginx-alpine:1.17.2

stages:
  - statgames

copy games:
  stage: statgames
  only:
    changes:
    - www/*
    - .gitlab-ci.yml
  script:
  - cp -a $CI_PROJECT_DIR/www/ /www-data/test/
